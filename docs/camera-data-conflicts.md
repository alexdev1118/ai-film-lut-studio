# Camera Data Conflicts

No open conflicts were recorded during the S13 Sony pilot.

A conflict must include the affected model and field, every competing fact ID, every source ID, an `open` or `resolved` state, and a written resolution when closed. Community observations never override an official fact; they remain visible as separate context.
