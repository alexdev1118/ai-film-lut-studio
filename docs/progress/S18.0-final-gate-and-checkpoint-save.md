# S18.0 Final Gate and Checkpoint Save

Date: 2026-07-17

## Download Gate

Chrome used `D:\Google Chrome下载` as its active download directory. A real browser request produced a temporary `.crdownload` and then a new Windows file, `POST_BT709_G24_FULL_2383_打印胶片启发_33pt_v1 (3).cube`.

- Print 2383 first download SHA-256: `0538694278f5dbd4e794a0e81ef0c3ad73695d110d0fc6bf6643f39d5b3c9b8f`
- Print 2383 repeat download SHA-256: identical
- Natural Skin download SHA-256: `ed6303332a4e00129d9e1807f7cd21b1fa06f98ca1f2882fb515b84bb8a998c9`
- Both downloaded files parsed and validated as 33-point Cube files with 35,937 RGB rows and a `0..1` domain.
- Both declare `Rec.709 / Gamma 2.4 / Full` input and output contracts and `Technical Conversion Included: false`.

The browser automation adapter did not capture a Blob download event. That limitation is recorded separately from the successful Windows filesystem evidence.

## Git Gate

- Remote: `https://github.com/alexdev1118/ai-film-lut-studio.git`
- `git fetch origin`: passed during this gate.
- `git ls-remote origin`: later failed with a transport-level GitHub connection timeout.
- `gh auth status`: reports the configured `alexdev1118` token as invalid.
- The checkpoint branch was pushed successfully with Git credential flow. The invalid GitHub CLI token did not prevent Git from fetching or pushing over the configured remote.

## File Audit

The explicit staging audit found no Git-visible local test media, downloaded Cubes, vendor LUT binaries, DPX/TIFF/video files, build outputs, node modules, caches, credentials, or user absolute-path artifacts. The copied browser-download verification files stay under ignored `local-test-assets/`.

## Local Checkpoint Commits

1. `55692a3` `feat(color): unify LUT contracts preview and DaVinci validation`
2. `49a1903` `feat(styles): add validated LUT styles and desktop comparison workflow`
3. `6089da6` `feat(product): add desktop quick mode export guidance and safety workflows`
4. `233d964` `feat(camera): add evidence-based camera profile registry`
5. `600aded` `docs(research): add desktop market research and S18 product strategy`

## Regression

Build, TypeScript, product workflow, color analysis, DPX, LUT pipeline, round-trip, camera data, LUT-strength, and style validation all passed in this gate. The DPX suite intentionally skipped its optional real-file fixture because `DPX_REAL_FILE` is not configured.

## Checkpoint Result

`checkpoint/s17.3-desktop-rc` was pushed to `origin`. The gate is complete: Windows download landing, file validation, repeat stability, different-style distinction, file audit, automated tests, logical commits, and remote checkpoint push all passed.
