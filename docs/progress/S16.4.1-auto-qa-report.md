# S16.4.1-AUTO-QA Report

## Execution Boundary

This run did not control DaVinci Resolve. The Computer Use runtime failed during bootstrap with the exact error `failed to write kernel assets: 系统找不到指定的路径。 (os error 3)`. No mouse, keyboard, UI Automation, project setting, render, LUT import, or file-export action was sent to DaVinci Resolve.

DaVinci Resolve Studio was detected as an existing process:

- Process: `Resolve` PID `27748`.
- Window title: `DaVinci Resolve Studio - Untitled Project 4`.
- Executable: local DaVinci Resolve installation (machine-specific path omitted).

Because the running project could be a user project, no attempt was made to save, duplicate, create a timeline, change project settings, import media, or render a still through any unsupported automation route.

## Static Readiness

- `local-test-assets/` is ignored by Git.
- The dedicated directories `cube`, `calibration`, `davinci`, `screenshots`, and `reports` were created under the ignored `local-test-assets/S16.4.1` directory.
- Vite development server was reachable at `http://127.0.0.1:5174/workspace` with HTTP 200.
- `npx tsc --noEmit` passed.
- `git diff --check` passed with line-ending notices only.
- The project bridge probe was attempted. `tools/davinci-bridge/scripts/export_current_frame.py` stopped explicitly because the bridge skeleton has no DaVinci API implementation. It created no still frame.

## Test A: Cube Calibration Round-Trip

Status: not executed.

- PRE path: not generated.
- POST path: not generated.
- Cube path: not generated.
- SHA-256: not available.
- DaVinci node chain: not inspected.
- Numeric metrics: not available.
- Viewer screenshot used for metrics: no.
- Pass result: no result.

Reason: Computer Use was unavailable before a separate safe test project could be created and before a unique Cube could be exported from the website into the ignored test directory.

## Test B: Sony S-Log3 / S-Gamut3.Cine Workflow

Status: not executed.

- CST settings: not inspected.
- PRE and POST paths: not generated.
- Numeric metrics: not available.
- Pass result: no result.
- Attribution: undetermined because Test A did not run.

## Test C: Six-Scene Generality Check

Status: not executed.

- No scene render was generated.
- No Key Gain value was inspected or altered.
- No visual risk conclusion was produced.

## Existing Real DPX Evidence

The two user-supplied desktop DPX files were not touched by this automation attempt. Independent parser and decoder checks previously established that they are valid, same-structure `SDPX`, Big Endian, `3840x2160`, 16-bit RGB, Packing 0, Encoding 0 frames. Strict decoded-preview edge comparison reports same-frame structure with edge MAE `0.010383` and similarity `0.989617`.

Their PRE/POST identity and the exact Cube remain unproven. Consequently, there is no real DaVinci Round-Trip numeric result and no evidence for a website LUT RGB algorithm defect.

## Code and Regression

- Core LUT RGB mapping was not changed.
- Cube sampling and trilinear interpolation were not changed.
- The only real-evidence correction in this worktree is the DPX real-file regression expectation: it now expects the observed 16-bit / Packing 0 source instead of an unsupported hard-coded 10-bit / Packing 1 assumption.
- No Git commit or push was performed.

## Required Follow-up

Repair the Computer Use runtime or provide a working DaVinci scripting bridge. Then create an isolated project named `AI_LUT_AUTOTEST_S16_4_1`, generate one unique Cube, and complete Test A before Test B or Test C.
